{
  "name": "fastify-lit-spa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "NODE_ENV=production node -r dotenv/config app/server/index.js",
    "server:dev": "NODE_ENV=development node -r dotenv/config app/server/index.js",
    "lint": "eslint --ignore-path .gitignore .",
    "lint:fix": "eslint --ignore-path .gitignore . --fix"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.26.0",
    "eslint-config-loopback": "^13.1.0"
  },
  "dependencies": {
    "dotenv": "^9.0.2",
    "fastify": "^3.15.1"
  }
}
